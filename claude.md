# Claude Interaction Notes

## Rules
1. **Never push changes to remote repositories without explicit user permission**
2. All changes must be committed locally first, then user will decide when/if to push

## Recent Changes
- Added `ScrollTrigger.min.js` (v1.0.5) to `static/js/` directory
- Updated `hugo.yml` to use local ScrollTrigger instead of CDN for better stability
- Commit: b4a7774 - "chore: add ScrollTrigger library to repo for stability"
